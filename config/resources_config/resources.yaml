backends:
  - name: litmus
    resource_types:
      - name: "experiment"
        actions:
          manage_experiment:
#            - team.experimenter
#            - owner
#            - two
  - name: odin
    resource_types:
      - name: "firehose"
        actions:
          manage:
            - team.firehose_manager