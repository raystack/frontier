rules:
  -
    frontend:
      url: "/hello.helloService/Hello"
      methods: ["POST"]
    backend:
      url: "http://localhost:50060/"
    middlewares:
      - name: authz
        config:
          action: "ping"
          attributes:
#            client:
#              type: grpc_payload
#              index: "2[1]"
            server:
              type: grpc_payload
              index: "1"