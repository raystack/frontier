rules:
  -
    frontend:
      url: "proto.v1.RuntimeService/"
      methods: ["POST"]
    backend:
      url: "http://localhost:8080/"
    middlewares: []
      # not supported at the moment
#      - name: authz # casbin based
#        config:
#          action: "ping"
#          attributes:
#            client:
#              type: grpc_payload
#              index: 1