run:
  timeout: 5m
output:
  format: colored-line-number
linters:
  enable-all: false
  disable-all: true
  enable:
    - goimports
    - thelper
    - unconvert
    - revive
    - unused
    - gofmt
    - whitespace
    - misspell
linters-settings:
  revive:
    ignore-generated-header: true
    severity: warning
severity:
  default-severity: error